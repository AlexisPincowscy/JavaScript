function executar(){
    console.log('Executou!')
    
}

const entrada = document.getElementById('botao')
// entrada.addEventListener('mousedown', executar) executa quando clica no botão do mouse
// entrada.addEventListener('mouseenter', executar) executa quando passamos o mouse por cima do elemento
// entrada.addEventListener('mouseleave', executar) executa APENAS QUANDO tiramos o cursos do elemento
// entrada.addEventListener('mousemove', executar) executa quando move o cursos em cima do elemento
// entrada.addEventListener('mouseout',executar) executa quando o mouse sai do elemento e do elemento filho (botao dentro de div, vai contar 2x)
// entrada.addEventListener('mouseover',executar) executa quando passa o cursos sobre o elemento
// entrada.addEventListener('mouseup',executar) executa quando terminamos o clique no botão do mouse, quando tirarmos o dedo do botão